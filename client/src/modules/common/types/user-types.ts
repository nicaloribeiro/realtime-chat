export type UsersFound = {
  email: string;
  name: string;
  username: string;
  friendRequestSent: boolean;
  isFriend: boolean;
};

export type UserSocketConnection = {
  email: string;
  name: string;
};
